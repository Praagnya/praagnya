# praagnya.github.io

Personal site (static HTML/CSS). Served by [GitHub Pages](https://pages.github.com/) from the `main` branch, `/` root.

**Enable Pages:** repo **Settings → Pages → Build and deployment → Source:** Branch `main`, folder `/ (root)`.

Site URL: `https://praagnya.github.io` (after the first successful deploy).
